{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

 {% block body %}
     <div {{ vue_component('UserProfile', {
        user: user_data,
        currentUser: app.user ? { id: app.user.id } : null,
    }) }}></div>
{% endblock %}
